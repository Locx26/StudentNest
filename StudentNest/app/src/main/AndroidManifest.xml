<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- REQUIRED: For Glide and Google Maps to fetch data -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- PROFESSIONAL ADDITION: Helps libraries detect if the user has internet -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".StudentNestApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.StudentNest"
        tools:targetApi="31">

        <!-- Ensure your Google Maps API Key is also here -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyD_OLwQ3nzbwbBXud67u-6eWTLp8Bv78go" />

        <!-- Your activities (LoginActivity, RegisterActivity, etc.) -->
        <activity android:name=".MainActivity" android:exported="true" android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.auth.RegisterActivity" />
        <activity android:name=".ui.listings.ListingsActivity" />
        <activity android:name=".ui.maps.MapsActivity" />
        <activity android:name=".ui.reservation.ReservationActivity" />
        <activity android:name=".ui.filter.FilterActivity" />

    </application>

</manifest>